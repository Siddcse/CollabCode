[phases.setup]
nixPkgs = ["nodejs_24", "python3", "jdk", "gcc", "gnumake", "go", "rustc"]

[phases.install]
commands = ["npm ci"]

[phases.build]
commands = ["npm run build"]

[start]
cmd = "node backend/dist/index.js"
